[package]
name = "database"
version = "1.0.0"
edition = "2024"
build = "build.rs"

[dependencies]
tabled = "0.21.0"
whoami = "2.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
disk-name = "1.0.0"
flate2 = "1.1"

[dev-dependencies]
tempfile = "3.27"

[build-dependencies]
serde_json = "1.0"
flate2 = "1.1"

[target.'cfg(windows)'.dependencies]
winreg = "0.56.0"
